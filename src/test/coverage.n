// nc -X coverage -X dumpast

include "machine_generic.n"

type wisp { };

struct xyz { int x; int y; int *z; };
union  abc { int a; int b; int c; };

struct llnode { int data; llnode *next; };

static int q[12] @ 27;
static int qq[12] @ symbol;
int r[12] @ 27 := { .x := 1, 1, 1 };
int s[12] @ 27 := { 1, 1, 1 };
int five := 5;
const int six := 6;

zp int in_the_zero_page := 5;

int a[5], *b, c := 3;

void (*fruitloops)(int n);

void useless() {
   static int q[12] @ 27;
   int r[12] @ 27 := { {0,0,0}, {0,0,0}, {0,0,0} };
   int r[12] @ 27 := { {0,0,0}, {0,0,0}, {0,0,0}, };
   int s[12] @ 27 := { 1, 1, 1 };
   int s[12] @ 27 := { 1, 1, 1, };
   int five := 5;
   const int six := 6;

   int a := 5 | 6;
   a := 5 & 6;
   a := 5 ^ 6;
   a := 8 ? 2 : 4;
   a ^= 12;

   foo := bar;

   a[12] := 5;
   a += b.c;

   a := (b || c) && d;

   a := a > b;
   a := a < b;
   a := a >= b;
   a := a <= b;
   a := a == b;
   a := a != b;

   a := a << 1;
   a := a >> 1;

   a := a % 0;

   ++a;
   --a;
   a++;
   a--;

   *a++;
   a->b;

   a := !a;
   a := ~a;
   a := -a;
   a := &a;

   a;
   ;

   a := 3.14;
   a := 1;
   a := "hello";

   // TODO FIX this is fugly!
   a := { 1, 2, 3 };
   a := { 3.14 };
   a := { -3.14 };
   a := { 3 };
   a := { -3 };
   a := { "hello" };

   useless();
   useless(1, 2, 3);

   return;
}

int globalint0;
int globalint1 := 5;

int operator*(int l, int r);

int operator*(int l, int r) {
   return l * r;
}

static const ref int operator+(const int l, int r);

static const ref int operator+(const int l, int r) {
   { // you know, for scoping!
      int q;
      int r := 7;
      q += 7;
      q -= 7;
      q *= 7;
      q /= 7;
      q %= 7;
      q <<= 1;
      q >>= 1;
   }
   return l + r;
}

int function0(void **);

int function0(void **) {
   return 5;
}

const int function1(int i);

const int function1(int i) {
   return 5;
}

int function2(int i @ 12, int j @ 13);

int function2(int i @ 12, int j @ 13) {
   static int q[12] @ 27;
   static int r[12] @ 27 := { 1, 1, 1 };
   switch (i+j) {
      case 0:
         i := i - j;
         break;
      case 1:
         i &= j;
         j |= i;
         break;
      default:
         break;
   }
}

int function3(void);

int function3(void) {

   if (3 + 3 == 6) {
   label:
      goto label;
   }
   else {
      return 1 + 2 - 3 * 4 / 5;
   }

   int i;

   for (i := 0; i < 10; i++) {
      if (i == 3) {
         continue;
      }
   }

   while_label:
   while (i != 5) {
      i++;
      break while_label;
   }

   do_label:
   do {
      i--;
      continue do_label;
      i++;
   } while (i != 5);
}
