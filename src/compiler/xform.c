#include <stdio.h>
#include <string.h>

#include "ast.h"
#include "xform.h"

int register_xform(ASTNode *xform) {
   return -1; // TODO FIX
}

const char *do_xform(const char *s, const char *xform) {
   return s; // TODO FIX
}
